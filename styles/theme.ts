import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#03045e",
    },
    text: {
      primary: "#000",
      secondary: "#fff",
    },
  },

  typography: {},
});

export default theme;
